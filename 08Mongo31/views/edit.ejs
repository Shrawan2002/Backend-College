<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit you  chat</title>
</head>
<body>
    <h3>Edit your chat: <%= chat._id %></h3>
    <p>This chat was sent from <b> <%= chat.from %> </b> to <b><%= chat.to %> </b></p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT ">
        <textarea rows="7" cols="30" name="msg"><%= chat.msg %></textarea>
        <br />
        <input type="date" name="updated_at" />
        <br/>
        <button style="padding: 8px; width: 100px; margin-top: 10px; background-color: #1e90ff;">Edit</button>
    </form>
</body>
</html>